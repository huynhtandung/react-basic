const styles = (theme) =>({
    card : {
        //backgroundColor : theme.color.primary
        marginBottom : '10px'
    },
    cardAction : {
        justifyContent: 'flex-end'
    }
});

export default styles;